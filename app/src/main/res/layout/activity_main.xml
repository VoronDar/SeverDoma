<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#FFFFFF"
    android:orientation="vertical">

    <ImageView
        android:id="@+id/imageView"
        android:src = "@drawable/bg"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="centerInside"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent">

    </ImageView>

    <TextView
        android:id="@+id/name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="#СеверДома"
        android:textColor="#464866"
        android:layout_marginBottom= "300dp"
        android:textSize="50sp"
        app:layout_constraintBottom_toTopOf="@id/OPEN"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <Button
        style = "@style/NextButton"
        android:id="@+id/OPEN"
        android:text="найти предприятия"
        android:layout_marginBottom="64dp"
        app:layout_constraintBottom_toTopOf="@id/SEND"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"/>

    <Button
        style = "@style/AddButton"
        android:id="@+id/moderator"
        android:layout_marginBottom="16dp"
        android:text="администрирование"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent" />

    <Button
        style = "@style/AddButton"
        android:id="@+id/SEND"
        android:layout_marginBottom="16dp"
        android:text="добавить предприятие"
        app:layout_constraintBottom_toTopOf="@id/moderator"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>